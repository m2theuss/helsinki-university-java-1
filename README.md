Normally when you set up the tmc from mooc.fi (in this case, presunption that you is using vs code with the extension named tmc)
First you must (as said before) download the extension "TMC", log in it using your mooc account select the course in this case "java 1" and download all itens clicking on donwload bottom.
After that locate where the tmc put the downloaded files, using right bottom of mouse in any folder of the workspace and click in "see in file explorer" or something like that.
So, locate it, delete all itens (folders in this case) but, IMPORTANT: DONT DELETE THE ROOT FOLDER! In my testes it's named "mooc-java-programming-i" YOU MUST use git clone INSIDE OF IT. So TMC can run the tests normally.

So, when you whant, use the terminal or git gui for upload it to your repositoy.

